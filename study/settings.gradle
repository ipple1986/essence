rootProject.name="study"
include 'algorithm'
include "servlet-filter-order"
include "cross-origin-resource-share"
include "loadbalance"
include "shua"

rootProject.children.each {
    it.buildFileName = it.name + ".gradle"
}
